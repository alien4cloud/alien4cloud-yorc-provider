#parse("$vtPath/instantiable_tosca_type.vm")
#if($type.validTargets.size() > 0)
${propertyUtils.indent($indent)}valid_target_types: [${yorcUtils.join($type.validTargets, ", ")}]
#end## if validTarget
